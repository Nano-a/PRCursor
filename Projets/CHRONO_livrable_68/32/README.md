# Tâche 32 — Fonctionnel : **poster** et **répondre**

**CHRONO N°32** | Branche : **`feature/etape1-proto-tcp`** | Qui : **M1, M2**

## Objectif

Membre du groupe : **POST** → `OK billet numb=…` ; **REPLY** sur un billet → `OK reply`.

## Référence code

Tâches **N°24–25** (`cmd_post`, `cmd_reply`, `handle_post`, `handle_reply`).

## Fichier extrait

| Fichier | Source |
|---------|--------|
| `extrait_scenario_post_reply.sh` | Chaîne `post` / `reply` — exige **`export U1 U3 IDG`** (ex. après tâche 31 jusqu’à `ans 1` pour U3). |

## Vérification

Après acceptation dans le groupe : `post` puis `reply … <numb> …`.

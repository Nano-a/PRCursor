# Branches — étape 1

| Branche | Rôle |
|---------|------|
| `main` | Version stable / rendu |
| `develop` | Intégration |
| `feature/etape1-proto-tcp` | Protocole TCP IPv6 sans TLS (étape 1) |

Les merges `--no-ff` depuis les features vers `develop` permettent de voir l’historique sur GitLab.
